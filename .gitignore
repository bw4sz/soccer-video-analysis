__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
*.pt
*.pth
runs/
*.mp4
!tests/fixtures/clip_10s_*.mp4
!docs/images/*.mp4
*.avi
*.mov
.ruff_cache/
.pytest_cache/
tests/results/

# SoccerNet NDA password (secret — never commit)
.soccernet_token

# Third-party training repos (vendored, not tracked)
vendor/

# SLURM job logs / per-run output dirs
slurm/logs/

# Local datasets / harvest downloads (large, regenerable — keep out of git)
data/

# Real squad rosters — other people's children, in a public repo. Keep the
# committed profiles to your own team's example (saints-u11.yaml) and leave
# full rosters untracked.
examples/profiles/saints-u14g.yaml

# Re-id galleries: real children's appearance embeddings, keyed by their names.
# Same reasoning as the rosters — regenerable from an annotation export anyway.
galleries/

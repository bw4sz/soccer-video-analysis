"""sn-calibration adapter (stub — requires Google Drive weights)."""

from __future__ import annotations


def is_available() -> bool:
    """Check if sn-calibration weights are accessible."""
    # Phase 5: check for downloaded weights
    return False

# Stub — full implementation in Phase 5
